string s = "abcdef";
// s.substr(first position, size);
string s2 = s.substr(3, 2); // s2 = "de"
// if the size is empty it takes the substring from first pos to the end
string s3 = s.substr(2); // s3 = "cdef"