#include <cctype>
isdigit(str[i]); // check if it's a number
isalpha(str[i]); // check if it's a letter
islower(str[i]); // check if it's lowercase
isupper(str[i]); // check if it's uppercase
isalnum(str[i]); // check if it's a number or letter
tolower(str[i]); // converts to lowercase
toupper(str[i]); // converts to uppercase