// XOR(L,R) = XOR(1,L-1) ^ XOR(1,R)
ans= pre = 0
Trie.insert(0)
for i=1 to N:
    pre = pre XOR a[i]
    Trie.insert(pre)
    ans=max(ans, Trie.query(pre))
print ans

// a funcao query é a mesma da maximum xor between two elements 